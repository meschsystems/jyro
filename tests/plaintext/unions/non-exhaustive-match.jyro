# Test that non-exhaustive match is a validation error

union Shape
    Circle(radius: number)
    Rect(width: number, height: number)
end

func Describe(s)
    match s do
        case Circle(r) then
            return "circle"
    end
end

Data.result = Describe(Circle(1))
